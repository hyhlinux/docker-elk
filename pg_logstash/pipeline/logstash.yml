input {
    jdbc {
        jdbc_connection_string => "jdbc:postgresql://192.168.1.104:5432/postgres"
        jdbc_user => "postgres"
        jdbc_password => "mysecretpassword"
        type => "db_apkpure_log"
        jdbc_validate_connection => true
        jdbc_driver_library => "./postgresql-42.1.4.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        statement => "SELECT * from post_post where timestamp > '2017-09-02 00:00:00' and timestamp < '2017-09-18 23:59:59'"
    }
}



output {
    stdout {
        codec => rubydebug
    }
    elasticsearch {
        hosts => ["192.168.1.104:9200"]
        index => "migrate_data-%{+YYYY.MM.dd}"
        document_type => "db_apkpure_log"
        codec => "json"
        sniffing => true
        template_overwrite => true
    }
}

